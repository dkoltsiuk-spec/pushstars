# Фаза 06 — Экран профиля и статистики

## Цель фазы

Реализовать вкладку **Профиль** по макету экрана 7: аватар (placeholder или Genies после фазы 10), имя, стрик, кнопка «Гардероб», переключатель упражнений (MVP только pushups disabled/enabled для будущего), график недели, три KPI (победы, винрейт, всего повторений), история матчей с фильтрами TYPE / MODE и пагинацией.

## Что НЕ делаем в этой фазе

- Полный магазин косметики (фаза 10).
- Редактор эталона движения (фаза 09).

## Предусловия

Фазы 03, 05.

## Затрагиваемые экраны

Экран 7.

## Файлы

- `Assets/_Project/Scripts/UI/Profile/ProfilePresenter.cs` (+ View)
- `Assets/_Project/Scripts/Data/MatchHistoryRepository.cs` — запрос `matches` `array-contains` + `OrderBy createdAt`
- `Assets/_Project/UI/Prefabs/Profile/` — карточки матчей W/L

Фильтры: клиентская фильтрация первой страницы или доп. поля в матче `matchType`, `gameMode` для серверного фильтра (предпочтительно поля в документе матча).

## Acceptance criteria

- [~] История подгружает 20 документов и «load more». ⚠️ `MatchHistoryRepository` (array-contains + пагинация, PageSize 20) написан и подключён; рендер строк/«load more» — когда появятся матчи (фазы 12–14). Пока empty-state.
- [~] Своя строка визуально выделена. ⚠️ Профиль показывает только свои матчи по построению (array-contains uid).
- [ ] График — отложено: нет источника данных (агрегаты/`dailyStats` — позже).

> **Сделано (фаза 06, первый срез):** вкладка Профиль в `Main.unity` (строит `MainVsScreenSetup.BuildProfilePanel`).
> Шапка (аватар-placeholder, имя, ранг, серия) + 3 KPI (Победы/Винрейт/Всего) биндятся из `users/{uid}`
> через `ProfilePresenter` → `UserProfileRepository` (Firestore read, дефолты при отсутствии дока).
> Проверено на живом доке: Денис/ЗОЛОТО/12/75%/540. Бэкенд: добавлен `ensureProfile` (идемпотентный backfill).
> **Отложено:** недельный график (нет источника), переключатель упражнений (MVP только pushups),
> рендер строк истории + «load more» (нет матчей), смена имени (нужен FirebaseFunctions SDK — фаза 07/09).
> ⚠️ Запускать игру со сцены **Boot** (иначе нет логина/сервисов — профиль покажет дефолты).

## Тестирование

Staging данные: 3+ матча win/loss.

## Связь с дизайном

Экран 7.
