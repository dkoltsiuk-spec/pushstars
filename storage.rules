rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Ghost-recording skeleton files — read by signed-in users, written only by
    // Cloud Functions (service account bypasses these rules).
    match /ghost/{uid}/{sessionId} {
      allow read: if request.auth != null;
      allow write: if false;
    }
  }
}
